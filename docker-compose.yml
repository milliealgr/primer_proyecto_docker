services:
  hola-mundo:
    build: 
      context: .
      dockerfile: dockerfile
    container_name: hola_mundo_node
    ports:
      - "3001:3000"
    environment:
      PORT: 3000
    restart: always
